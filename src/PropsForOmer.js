import React from 'react';

const PropsForOmer = ({name,username}) => {
    // const {name,username} = props
    return(
        <div>
            <h2> name: {name} </h2>
        </div>
    );
}
export default PropsForOmer;